module.exports = function(fromFile, callback) {
  // Determine format
  // Convert to the other format. Use a file in /tmp or similar, do NOT crush the other test case file (:
  // Generate thumbnail
  // Invoke callback with null and an object containing paths to all three

  // If you were going to cheat the unit tests semi-successfully you might do this:
  // return callback(null, {
  //   mp4: '/tmp/test.mp4',
  //   webm: '/tmp/test.webm',
  //   jpg: '/tmp/test.jpg'
  // });
};
